type: object
required:
  - tradeInstrumentIds
properties:
  tradeInstrumentIds:
    type: array
    description: IDs of trade objects matching the query.
    items:
      $ref: "#/components/schemas/TradeInstrumentId"
