description: A list of event types. Default is all events
example:
  - EQUIPMENT
  - SHIPMENT
  - TRANSPORT
default:
  - EQUIPMENT
  - SHIPMENT
  - TRANSPORT
type: array
items:
  $ref: "#/components/schemas/eventType"
