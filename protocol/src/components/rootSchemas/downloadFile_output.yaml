type: array
items:
  type: object
  properties:
    MessageId:
      type: string
      description: The message id of the file.
    Subfolder:
      type: string
      description: The subfolder of the file.
    File:
      type: string
      description: The name of the file.
    FileSize:
      type: string
      description: The size of the file.
    TimeCreated:
      type: string
      description: The time the file was created.
    ErrorMessage:
      type: string
      description: >-
        When multiple files are transferred over the same connection, this will
        return the error message if an individual file transfer failed. If this
        is empty, the file transfer was successful.
